type Query {
  orders: [Order]
}

type Order {
  id: ID!
  product: Product!
  quantity: Int!
  date: String!
  subtotal: Float!
  items: [OrderItem!]!
}

type OrderItem {
  product: Product!
  quantity: Int!
}
